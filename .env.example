# Optional: lock the whole site behind HTTP Basic Auth (set these in Vercel → Settings → Environment Variables).
# Omit PRIVATE_SITE_PASSWORD locally so `next dev` stays open.
PRIVATE_SITE_USER=preview
PRIVATE_SITE_PASSWORD=qqEK6DgRtk
